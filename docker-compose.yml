services:
  lukasfoto:
    build: .
    container_name: lukasfoto
    restart: unless-stopped
    volumes:
      - ./src:/var/www/html
      - ./uploads:/var/www/html/uploads
      - ./uploads.ini:/usr/local/etc/php/conf.d/uploads.ini
    environment:
      DB_HOST: db
      DB_USER: ${MYSQL_USER}
      DB_PASS: ${MYSQL_PASSWORD}
      DB_NAME: fotobox
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lukasfoto.rule=Host(`lukasfoto.klimarschanlage.de`)"
      - "traefik.http.routers.lukasfoto.entrypoints=https"
      - "traefik.http.routers.lukasfoto.tls=true"
      - "traefik.http.routers.lukasfoto.tls.certresolver=letsencrypt"
      - "traefik.docker.network=traefik"

      - "traefik.http.routers.lukasfoto-admin.rule=Host(`lukasfoto.klimarschanlage.de`) && Path(`/info.php`)"
      - "traefik.http.routers.lukasfoto-admin.entrypoints=https"
      - "traefik.http.routers.lukasfoto-admin.tls.certresolver=letsencrypt"
      - "traefik.http.routers.lukasfoto-admin.priority=100"
      - "traefik.http.routers.lukasfoto-admin.middlewares=authentikAuthKlimarsch@file"
    networks:
      - default
      - traefik

  db:
    image: mariadb:10.11
    container_name: lukasfoto_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: fotobox
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - ./db_data:/var/lib/mysql
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

networks:
  traefik:
    external: true